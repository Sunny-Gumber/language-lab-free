{
  "name": "language-lab-free",
  "private": true,
  "version": "14.0.1",
  "type": "module",
  "description": "Guided adaptive language-learning platform with connected input, retrieval, free response, deepening Japanese/Mandarin paths and eight foundation courses",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js",
    "ci": "npm run check && npm test",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}