{
  "name": "language-lab-free",
  "private": true,
  "version": "12.0.0",
  "type": "module",
  "description": "Mobile-first language learning app with a first-visit start experience and IndexedDB-backed event sync",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js",
    "ci": "npm run check && npm test",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
