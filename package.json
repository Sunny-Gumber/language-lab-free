{
  "name": "language-lab-free",
  "private": true,
  "version": "15.1.0",
  "type": "module",
  "description": "Guided adaptive language-learning platform with connected input, interleaved retrieval, free response, deepening Japanese/Mandarin paths and eight foundation courses",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js",
    "ci": "npm run check && npm test && npm run course-packs:check",
    "e2e": "playwright test",
    "course-packs:check": "node scripts/build-course-packs.js",
    "course-packs:build": "node scripts/build-course-packs.js --write"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
