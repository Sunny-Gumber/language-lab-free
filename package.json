{
  "name": "language-lab-free",
  "private": true,
  "version": "11.0.0",
  "type": "module",
  "description": "Mobile-first language learning app with event-based Supabase sync",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js",
    "ci": "npm run check && npm test"
  }
}
