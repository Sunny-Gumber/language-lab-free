{
  "name": "language-lab-free",
  "private": true,
  "version": "13.1.0",
  "type": "module",
  "description": "Mobile-first progressive language learning app with adaptive mixed sessions, retrieval practice and IndexedDB-backed event sync",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js",
    "ci": "npm run check && npm test",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}