{
  "name": "language-lab-free",
  "private": true,
  "version": "10.0.0",
  "description": "Static multi-language learning app with optional Supabase sync",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-syntax.js",
    "ci": "npm run check && npm test"
  }
}
